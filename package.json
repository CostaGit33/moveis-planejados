{
  "name": "moveis-planejados",
  "version": "0.1.0",
  "private": true,
  "description": "MVP local para projeto e orcamento de moveis planejados.",
  "scripts": {
    "start": "node api/server.js",
    "check": "node --check api/server.js && node --check api/db.js && node --check api/mvp-routes.js && node --check api/n8n-routes.js && node --check furniture-builder.js && node --check server.js && node --check public/app.js",
    "gerar:poc": "python gerar_poc.py"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "pg": "^8.23.0"
  }
}
